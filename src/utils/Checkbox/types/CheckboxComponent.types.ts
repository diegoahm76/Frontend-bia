//  const { checked, condition, title1, title2, handleChange } = params;

export interface CheckBoxTypes {
  checked: boolean
  condition: boolean,
  title1: string,
  title2: string,
  handleChange: Function,
}